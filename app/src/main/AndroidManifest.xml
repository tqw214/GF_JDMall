<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.viger.gfJdmall">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".application.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".activity.LoginActivity" android:launchMode="singleTop"/>
        <activity android:name=".activity.MainActivity" android:launchMode="singleTask">

        </activity>
        <activity android:name=".activity.RegistActivity" />
        <activity android:name=".activity.ProductListActivity" android:launchMode="singleTop"/>
        <activity android:name=".activity.ProductDetailActivity">

        </activity>
        <activity android:name=".activity.SettleActivity">
        </activity>
        <activity android:name=".activity.AddReciverActivity">
        </activity>
        <activity android:name=".activity.ChooseAddressActivity"/>
        <activity android:name=".activity.OderListActivity"/>
        <activity android:name=".activity.AlipayActivity"/>

    </application>

</manifest>